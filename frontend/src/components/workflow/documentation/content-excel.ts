export const excelGuideContent = `# Excel与数据表格

本章详细介绍 Excel 文件操作和数据表格功能。

---

## Excel文件资源

### 什么是Excel文件资源？

Excel文件资源是上传到系统中的 Excel 文件，可以在工作流中读取和使用。

### 上传Excel文件

1. 点击顶部工具栏的 **Excel资源** 按钮
2. 点击 **上传文件**
3. 选择 .xlsx 或 .xls 文件
4. 文件上传后可在工作流中使用

### 支持的格式

| 格式 | 说明 |
|------|------|
| .xlsx | Excel 2007+ 格式（推荐） |
| .xls | Excel 97-2003 格式 |

### 文件管理

- **预览**：点击文件可预览内容
- **删除**：删除不需要的文件
- **刷新**：重新加载文件列表

---

## 读取Excel

### 基本配置

| 参数 | 说明 |
|------|------|
| 文件 | 选择已上传的Excel文件 |
| 工作表 | 选择要读取的工作表 |
| 保存到变量 | 存储数据的变量名 |

### 数据格式

读取后的数据是一个**列表**，每行是一个**字典**：

**Excel内容**：
| 姓名 | 年龄 | 城市 |
|------|------|------|
| 张三 | 25 | 北京 |
| 李四 | 30 | 上海 |

**读取后的变量**：
\`\`\`json
[
  {"姓名": "张三", "年龄": 25, "城市": "北京"},
  {"姓名": "李四", "年龄": 30, "城市": "上海"}
]
\`\`\`

### 使用数据

**遍历所有行**：
\`\`\`
读取Excel -> 保存到：数据列表

遍历列表：数据列表
  ├─ 打印日志：姓名={item[姓名]}，年龄={item[年龄]}
  └─ 其他操作...
\`\`\`

**访问特定行**：
\`\`\`
{数据列表[0]}        -> 第一行数据
{数据列表[0][姓名]}  -> 第一行的姓名
{数据列表[-1]}       -> 最后一行数据
\`\`\`

---

## 数据表格

数据表格是 WebRPA 内置的数据收集功能，用于在工作流执行过程中收集和管理数据。

### 查看数据表格

点击底部日志面板的 **数据表格** 标签页。

### 功能特点

| 功能 | 说明 |
|------|------|
| 自动收集 | 配置"存储的列名"后自动收集 |
| 实时预览 | 执行过程中实时显示数据 |
| 手动编辑 | 可以手动修改单元格内容 |
| 导出CSV | 一键导出为CSV文件 |

---

## 数据表格操作模块

### 添加行

向数据表格添加一行数据。

**配置**：
| 参数 | 说明 | 示例 |
|------|------|------|
| 行数据 | JSON格式的行数据 | \`{"姓名": "{name}", "价格": "{price}"}\` |

**示例**：
\`\`\`json
{
  "商品名": "{商品名称}",
  "价格": "{商品价格}",
  "链接": "{商品链接}"
}
\`\`\`

### 添加列

向数据表格添加新列。

**配置**：
| 参数 | 说明 |
|------|------|
| 列名 | 新列的名称 |
| 默认值 | 新列的默认值（可选） |

### 设置单元格

修改指定单元格的值。

**配置**：
| 参数 | 说明 | 示例 |
|------|------|------|
| 行索引 | 行号（从0开始） | 0、{index} |
| 列名 | 列的名称 | 状态 |
| 值 | 要设置的值 | 已处理 |

### 读取单元格

获取指定单元格的值。

**配置**：
| 参数 | 说明 |
|------|------|
| 行索引 | 行号（从0开始） |
| 列名 | 列的名称 |
| 保存到变量 | 存储值的变量名 |

### 删除行

删除指定行。

**配置**：
| 参数 | 说明 |
|------|------|
| 行索引 | 要删除的行号 |

### 清空表格

清空所有数据。

### 导出表格

将数据表格导出为文件。

**配置**：
| 参数 | 说明 | 示例 |
|------|------|------|
| 文件路径 | 保存位置 | C:/data/result.csv |
| 文件格式 | CSV 或 Excel | csv |

---

## 数据采集工作流

### 基本模式

\`\`\`
打开网页
  下
循环执行（翻页）
  ├─ 获取元素信息（配置列名自动收集）
  ├─ 点击下一页
  └─ 等待加载
  下
导出表格
\`\`\`

### 自动收集 vs 手动添加

**自动收集**（推荐）：
在"获取元素信息"模块中配置"存储的列名"，数据自动添加到表格。

**手动添加**：
使用"添加行"模块手动构建数据行。

### 示例：采集商品列表

\`\`\`
1. 打开网页：商品列表页
2. 循环执行 10 次（采集10页）
   ├─ 遍历列表：商品元素
   │   ├─ 获取元素信息：商品名 -> 列名"名称"
   │   ├─ 获取元素信息：价格 -> 列名"价格"
   │   └─ 获取元素信息：链接 -> 列名"链接"
   ├─ 点击下一页
   └─ 等待页面加载
3. 导出表格：C:/采集结果.csv
4. 播放提示音
\`\`\`

---

## 导出数据

### CSV格式

- 通用格式，Excel可直接打开
- 支持中文（UTF-8编码）
- 适合数据交换

### 手动导出

1. 打开数据表格面板
2. 点击 **下载CSV** 按钮
3. 选择保存位置

### 自动导出

使用"导出表格"模块在工作流中自动导出：
\`\`\`
导出表格
  文件路径：C:/data/{日期}_结果.csv
  格式：CSV
\`\`\`

---

## 最佳实践

### 数据去重

\`\`\`
遍历采集的数据
  ├─ 条件判断：是否已存在
  │   ├─ 是 -> 跳过
  │   └─ 否 -> 添加到表格
\`\`\`

### 数据验证

\`\`\`
获取数据后
  ├─ 条件判断：数据是否有效
  │   ├─ 是 -> 保存
  │   └─ 否 -> 记录错误日志
\`\`\`

### 增量采集

\`\`\`
1. 读取已有数据（Excel文件）
2. 采集新数据
3. 对比去重
4. 追加新数据
5. 导出更新后的文件
\`\`\`

### 大数据量处理

- 分批采集，每批导出一次
- 避免内存占用过大
- 定期清空数据表格

---

## 常见问题

### Excel读取乱码

**原因**：文件编码问题
**解决**：确保Excel文件是标准格式，避免特殊字符

### 数据丢失

**原因**：工作流中断未保存
**解决**：
- 定期导出数据
- 使用"导出表格"模块自动保存

### 列名不匹配

**原因**：Excel列名与代码中不一致
**解决**：检查Excel第一行的列名，确保完全匹配

### 数字变成文本

**原因**：Excel单元格格式问题
**解决**：在Excel中设置正确的单元格格式

---

## Excel 自动化模块（直接读写 .xlsx，基于 openpyxl）

除了"上传资源 + 读取Excel"，WebRPA 还提供**一整套直接操作本地 .xlsx 文件**的自动化模块（侧边栏「Excel自动化」分类），无需上传资源、按文件路径即可读写，覆盖几乎所有 openpyxl 能做的操作。

### 工作簿与工作表
| 模块 | 作用 |
|------|------|
| 创建Excel | 新建工作簿（可指定多个工作表名） |
| 添加/删除/重命名工作表 | 工作表增删改 |
| 复制工作表 / 移动工作表 | 复制一份 / 调整顺序 |
| 列出工作表 / 获取表格信息 | 拿工作表名列表 / 行列数维度 |
| 激活工作表 / 工作表标签色 | 设默认显示表 / 标签着色 |
| 清空工作表 / 另存为Excel | 清空内容 / 复制到新路径 |

### 单元格与区域读写
| 模块 | 作用 |
|------|------|
| 写入单元格 / 读取单元格 | 单格读写（如 A1） |
| 写入区域 / 读取区域 | 二维数组批量读写 |
| 写入字典数组 / 读取为字典数组 | 带表头的结构化读写（最常用） |
| 追加行 / 填充内容 / 复制区域 / 清空区域 | 行追加、区域填充/复制/清空 |
| 查找替换 / 读取总行数 | 文本替换 / 统计行数 |
| 获取第一个空行/空列/空单元格 | 找追加位置 |

### 行列 / 公式 / 样式
| 模块 | 作用 |
|------|------|
| 插入/删除 行、列 | 行列增删 |
| 隐藏行列 / 设置行高列宽 / 冻结窗格 | 显隐、尺寸、冻结 |
| 设置公式 / 读取公式或计算值 | 写 =SUM(...) / 读公式或缓存值 |
| 合并单元格 / 设置样式 / 设置边框 | 合并、字体颜色对齐、边框 |
| 数字格式 / 删除样式 | 日期/货币/百分比格式 / 清样式 |

### 高级
| 模块 | 作用 |
|------|------|
| 插入图片 / 插入图表 | 图片、柱/折线/饼图等 |
| 数据验证 / 条件格式 | 下拉列表/数值约束 / 高亮规则、色阶、数据条 |
| 自动筛选 / 区域排序 / 删除重复行 | 筛选、按列排序、去重 |
| 数据透视表 | 按字段分组聚合生成汇总表 |
| 导出CSV / CSV转Excel | 与 CSV 互转 |
| 保护工作表 / 页面设置 / 视图缩放 | 加密保护 / 打印纸张方向 / 缩放网格线 |
| 导出为PDF / 运行Excel宏 / 刷新Excel数据 | 基于 Excel/WPS（需安装 Office/WPS，仅 Windows） |

> 提示：导出PDF、运行宏、刷新数据这三个模块通过 COM 操作真实的 Excel/WPS 应用，需要本机安装 Office 或 WPS；其余模块均为纯文件操作，无需安装 Office。

---

## 数据表格性能（大数据量）

底栏「数据表格」采用**虚拟滚动**渲染——无论收集了多少条（上万条也不卡），DOM 里始终只渲染可视区域的行。

- **全量展示**：不再限制预览条数，有多少显示多少。
- **实时填充**：执行过程中数据分批实时推送到表格。
- **完整下载**：点右上角「下载数据」导出的是**全部**收集到的数据（不止可见部分），即使超出预览也完整保存。
---

## WPS多维表格

对标飞书多维表格，接入金山 WPS 开放平台（open.wps.cn）的多维表格（dbsheet）能力。需先在 WPS 开放平台创建应用，获取 AK / SK。

| 模块 | 说明 |
|------|------|
| **wps_bitable_write** | 向多维表格写入记录 |
| **wps_bitable_read** | 读取多维表格全部记录（自动翻页） |

公共参数：AK、SK、文件ID（fileId）、表ID（sheetId）；baseUrl 可按平台文档微调，默认 https://openapi.wps.cn 。

写入支持两种数据源：手动填写字段，或从变量读取（字典写入一条，字典列表批量写入）。读取会自动按每页 200 条翻页，把全部记录存入结果变量（默认 wps_data）。
`
