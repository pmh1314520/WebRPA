export const browserGuideContent = `# 自动化浏览器详解

本章详细介绍自动化浏览器的工作原理、配置方法和使用技巧。

---

## 浏览器引擎架构

WebRPA 采用**主进程浏览器引擎**架构，所有工作流执行时共享同一个浏览器实例。

### 架构优势

| 优势 | 说明 |
|------|------|
| 高效率 | 无需重启浏览器，直接复用已打开的页面 |
| 低资源 | 共享浏览器实例，节省系统资源 |
| 多页面 | 支持多页面操作，自动管理页面生命周期 |
| 状态保持 | 登录状态、Cookie 等自动保持 |
| 异步执行 | 所有操作都是异步的，不阻塞主线程 |

### 工作流程

1. **启动浏览器**：点击工具栏"打开浏览器"按钮
2. **手动操作**：在浏览器中手动打开网站、登录等
3. **创建工作流**：第一个模块使用"操作已打开的网页"
4. **执行工作流**：直接操控已打开的页面，无需重启

---

## 什么是自动化浏览器？

WebRPA 使用 **Playwright** 驱动浏览器执行自动化任务。默认使用 **Microsoft Edge**，也支持 **Chrome**、**Chromium** 和 **Firefox**。

### 核心特点

| 特点 | 说明 |
|------|------|
| 真实浏览器 | 使用真实的浏览器，而非模拟器 |
| 多浏览器支持 | 支持 Edge、Chrome、Chromium、Firefox |
| 登录状态保持 | 浏览器数据持久化，登录状态自动保存 |
| 可视化操作 | 可以看到浏览器执行过程 |
| 元素选择器 | 支持可视化选择网页元素 |
| 共享实例 | 所有工作流共享同一个浏览器实例 |

### 浏览器数据目录

浏览器的所有数据（Cookie、登录状态、缓存等）保存在：
\`\`\`
backend/browser_data/{浏览器类型}/
\`\`\`

例如：
- Edge: \`backend/browser_data/msedge/\`
- Chrome: \`backend/browser_data/chrome/\`
- Firefox: \`backend/browser_data/firefox/\`

这意味着：
- 登录一次网站后，下次运行工作流无需重新登录
- 不同浏览器的数据相互独立
- 可以手动清理该目录来重置浏览器状态

---

## 浏览器配置

### 切换浏览器类型

1. 点击顶部工具栏的 **全局配置** 按钮
2. 选择 **浏览器** 标签页
3. 选择你想使用的浏览器类型：
   - **Microsoft Edge**（默认）：Windows 系统自带
   - **Google Chrome**：需要安装 Chrome 浏览器
   - **Chromium**：Chrome 的开源版本
   - **Firefox**：需要安装 Firefox 浏览器

4. 如果浏览器无法启动，可以手动指定浏览器可执行文件路径

### 注意事项

- 修改浏览器配置后，需要**关闭并重新打开**浏览器才能生效
- 不同浏览器的数据目录是独立的，切换浏览器后需要重新登录网站
- 推荐使用 Edge 或 Chrome，兼容性最好

---

## 打开自动化浏览器

### 方式一：工具栏按钮

点击顶部工具栏的 **自动化浏览器** 按钮，可以：
- 打开/关闭浏览器
- 导航到指定网址
- 启动元素选择器

### 方式二：运行工作流

当工作流中包含"打开网页"模块时，会自动启动浏览器。

---

## 元素选择器

元素选择器是 WebRPA 的核心功能，让你无需编写 CSS 选择器就能定位网页元素。

### 启动方式

1. **工具栏启动**：点击"自动化浏览器" -> "启动选择器"
2. **配置面板启动**：点击选择器输入框旁的 按钮

### 选择模式

| 操作 | 功能 | 使用场景 |
|------|------|----------|
| **Ctrl + 点击** | 选择单个元素 | 选择按钮、输入框等 |
| **Alt + 点击** | 选择相似元素 | 选择列表项、表格行等 |
| **Esc** | 取消选择 | 重新选择 |

### 单元素选择

1. 启动选择器
2. 按住 **Ctrl** 键
3. 点击目标元素
4. 选择器自动填入配置

### 相似元素选择

用于选择多个结构相似的元素（如商品列表）：

1. 启动选择器
2. 按住 **Alt** 键
3. 点击第一个元素
4. 点击第二个相似元素
5. 系统自动识别所有相似元素

**生成的选择器示例**：
\`\`\`css
.product-list > div:nth-child({index})
\`\`\`

其中 \`{index}\` 是变量，配合循环使用可遍历所有元素。

---

## 页面导航

### 打开网页

\`\`\`
模块：打开网页
网址：https://www.example.com
等待条件：load（页面完全加载）
\`\`\`

### 等待条件详解

| 条件 | 说明 | 适用场景 |
|------|------|----------|
| load | 等待所有资源加载完成 | 普通网页 |
| domcontentloaded | DOM 加载完成即可 | 快速响应 |
| networkidle | 网络空闲 | Ajax 动态页面 |

### 页面跳转

| 模块 | 功能 |
|------|------|
| 刷新页面 | 重新加载当前页面 |
| 返回上一页 | 浏览器后退 |
| 前进下一页 | 浏览器前进 |
| 关闭页面 | 关闭当前标签页 |

---

## 处理弹窗

### 浏览器原生弹窗

处理 \`alert\`、\`confirm\`、\`prompt\` 弹窗：

| 弹窗类型 | 处理方式 |
|----------|----------|
| alert | 点击确定 |
| confirm | 点击确定或取消 |
| prompt | 输入文本后确定 |

**配置示例**：
\`\`\`
模块：处理弹窗
操作：确认（点击确定）
输入文本：验证码123（仅 prompt 需要）
\`\`\`

### 网页自定义弹窗

网页自定义的弹窗（非浏览器原生）需要用普通的点击操作处理：
\`\`\`
1. 等待元素：.modal-dialog 出现
2. 点击元素：.modal-confirm-btn
\`\`\`

---

## 性能优化

### 减少等待时间

\`\`\`
[×] 不推荐：固定等待 5000 毫秒
[√] 推荐：等待元素 #content 出现
\`\`\`

### 合理设置超时

| 场景 | 建议超时 |
|------|----------|
| 普通页面加载 | 30秒 |
| 复杂页面/慢网络 | 60秒 |
| 元素等待 | 10秒 |
| 文件下载 | 根据文件大小 |

### 避免频繁操作

\`\`\`
[×] 不推荐：
点击 -> 立即获取数据

[√] 推荐：
点击 -> 等待元素出现 -> 获取数据
\`\`\`

---

## 反检测说明

WebRPA 使用真实浏览器，但某些网站可能检测自动化行为。

### 降低检测风险

1. **添加随机延迟**：操作之间加入随机等待
2. **模拟人类行为**：不要操作过快
3. **保持登录状态**：使用持久化的浏览器数据
4. **合理的请求频率**：避免短时间大量请求

### 示例：添加随机延迟

\`\`\`
循环执行
  ├─ 执行操作
  ├─ 随机数：1000 到 3000
  └─ 等待：{随机数} 毫秒
\`\`\`

---

## 网页智能录制（录制即生成节点）

不想手动连节点？直接在浏览器里操作，WebRPA 自动录成工作流节点。

### 使用

\`\`\`
1. 先打开自动化浏览器
2. 顶栏「录制」下拉 ->「网页智能录制」
3. 点「开始录制」，然后在浏览器里正常操作
4. 点「停止录制」-> 按需编辑 ->「生成节点」追加到画布
\`\`\`

### 能录到什么

\`\`\`
- 点击、输入、下拉选择、勾选
- 滚动（自动合并方向与距离）
- 功能键 / 组合键（回车、Tab、方向键、Ctrl+C 等）
- 页面导航（打开/跳转网址）
\`\`\`

### 跨页面 / 新标签页不中断

事件由浏览器实时直推到后端，**点击链接跳转、打开新标签页、跨域跳转都会持续录制**，不会中途丢失。

### 录制后可编辑

\`\`\`
停止录制后，每一步支持：
- 删除该步
- 上移 / 下移调整顺序
勾选「自动等待」后，会按操作间隔自动插入等待节点（应对加载慢的页面）。
\`\`\`

> 录制生成的是标准节点，可以像手搭的工作流一样继续修改、加条件/循环。

---

## 常见问题

### 浏览器打不开

**可能原因**：
- 选择的浏览器未安装
- 端口被占用
- 权限不足
- 自定义路径不正确

**解决方法**：
1. 确保安装了对应的浏览器（Edge/Chrome/Firefox）
2. 关闭其他占用浏览器的程序
3. 以管理员身份运行
4. 在全局配置中检查浏览器路径设置
5. 尝试切换到其他浏览器类型

### 元素找不到

**可能原因**：
- 选择器不正确
- 元素在 iframe 中
- 元素动态加载

**解决方法**：
1. 使用元素选择器重新选择
2. 添加等待元素模块
3. 检查是否需要切换 iframe

### 登录状态丢失

**可能原因**：
- 浏览器数据目录被清理
- 网站 Cookie 过期

**解决方法**：
1. 重新登录一次
2. 检查 browser_data 目录是否存在

---

## 最佳实践

### 工作流开始

\`\`\`
1. 打开网页
2. 等待关键元素出现
3. 检查登录状态
4. 开始执行任务
\`\`\`

### 工作流结束

\`\`\`
1. 保存数据
2. 打印完成日志
3. （可选）关闭页面
4. （可选）播放提示音
\`\`\`

### 错误处理

\`\`\`
条件判断：{元素存在} == true
  ├─ 真 -> 继续执行
  └─ 假 -> 打印错误日志 -> 跳出循环
\`\`\`
---

## DP 反检测自动化

DrissionPage（DP）以“控制真实浏览器内核 + 收发数据包”的方式工作，对很多检测 webdriver 特征的网站更隐蔽，适合常规自动化浏览器被风控拦截的场景。

| 模块 | 说明 |
|------|------|
| **dp_open_page** | 打开 / 跳转页面，可选无头模式 |
| **dp_click** | 点击元素（支持超时） |
| **dp_input** | 输入文本，可选先清空 |
| **dp_get_text** | 获取元素文本并存入变量 |
| **dp_get_html** | 获取当前页面 HTML 存入变量 |
| **dp_run_js** | 执行 JavaScript，结果存入变量 |
| **dp_wait_element** | 等待元素出现 |
| **dp_scroll** | 滚动页面（bottom / top / down / up） |
| **dp_close** | 关闭 DrissionPage 浏览器 |

定位符直接透传 DrissionPage 语法：#id、.class、tag:xx、text:xx、xpath://...、css:...。

使用流程：先用 dp_open_page 打开页面（同一工作流内会复用一个浏览器会话），再用 dp_click / dp_input 等操作，最后用 dp_close 关闭。工作流结束时 DP 浏览器会自动关闭，下次运行始终是干净会话，不会复用上次残留；若上次页面已失活也会自动重建。

浏览器内核：DrissionPage 同样基于 Chromium，Edge 和 Chrome 都支持。dp_open_page 默认优先使用系统 Edge（与 WebRPA 其它模块一致），也可在配置里选择 Chrome 或填写浏览器路径；采集类任务可把"复用浏览器"设为"每次打开干净会话"。首次使用需安装依赖：在 WebRPA 目录执行 Python313/python.exe -m pip install DrissionPage。
`
