export const platformGuideContent = `# 🧰 平台能力（导出 / 凭据 / 版本 / 远程存储）

本章汇总 WebRPA 的平台级能力：把工作流导出成脚本或整包、加密保管口令、版本历史与回放、
WebDAV 远程存储、自定义快捷键、AI 操作权限等。

---

## 🆕 近期新增能力速览

- **版本历史（含全局变量）**：Git 式本地快照，记录节点、连线与**全局变量**，可随时恢复/对比；AI 小助手会在大改动前自动存档。
- **WebDAV 远程存储**：在「全局配置 → 存储」中配置 WebDAV，工作流可保存/读取到 NAS、Nextcloud、坚果云等，实现多端共享。
- **同名工作流自动副本**：开启后保存遇同名不再覆盖，而是自动另存带时间戳的副本。
- **AI 操作权限（三档）**：逐项确认 / 智能放行 / 自由执行；AI 操作前按风险弹授权，拒绝不会中断任务。
- **永不白屏**：全局错误边界兜底，任何模块异常都弹出错误卡片（含报错详情与一键 AI 诊断）。
- **自定义快捷键**：在「全局配置 → 系统」里给运行/停止/保存/新建等常用功能绑定组合键。
- **DrissionPage 反检测模块**：dp_ 系列模块以真实浏览器内核驱动，更易绕过风控站点。
- **插件市场**：「工作流仓库 → 插件」可浏览/安装第三方插件，详见「插件市场与开发」文档。
- **中英文国际化**：编辑器与启动器首次按系统语言自动切换，右上角可手动切换。

---

## 📤 多目标脚本导出

工作流可以导出成可独立运行的脚本，方便交给不同技术栈的团队接手。

### 入口

\`\`\`
顶栏「更多操作」或「导出」按钮 → 导出对话框 → 选择格式
\`\`\`

### 支持的格式

\`\`\`
- Playwright Python   ：可独立运行的 Playwright + Python 脚本（推荐）
- Selenium Python     ：Selenium + Python 脚本，适合习惯 Selenium 的团队
- Playwright JavaScript：Playwright + Node.js(JS) 脚本，适合前端/Node 团队
- JSON 格式           ：完整工作流配置，便于再次导入
- Markdown 文档       ：可读的流程说明
- 加密分享包          ：AES-256 加密，需密码才能导入
- 整包（含依赖）      ：见下一节
\`\`\`

### 覆盖范围说明

脚本导出覆盖网页操作（打开/点击/输入/下拉/勾选/键盘）、等待、滚动、截图、导航、
流程控制（条件/循环/遍历/跳出/继续）、变量与日志等核心节点。

\`\`\`
对于 Selenium / Playwright-JS 暂不支持的高级模块（系统操作、Excel、AI、手机等），
导出时会生成清晰的注释占位并附上原配置，提示在 WebRPA 内运行或手动实现，
不会静默丢弃，方便人工补全。
\`\`\`

---

## 📦 工作流整包（跨机器迁移 / 团队共享）

普通 JSON 导出只含工作流本身。**整包**会把工作流连同它依赖的「自定义模块」「图片资产」
一起打包，换台电脑或发给同事时一次性还原，节点引用不断裂。

### 导出整包

\`\`\`
导出对话框 → 选「整包（含依赖）」→ 下载 xxx.bundle.json
\`\`\`

### 导入整包

\`\`\`
顶栏「更多操作」→「导入整包」→ 选择 .bundle.json
- 自动还原依赖的自定义模块与图片（已存在则跳过，保持原 id）
- 工作流加载到画布，引用关系保持不变
\`\`\`

---

## 🔑 加密凭据库

邮件、API、数据库等口令不要写进工作流明文里（一旦分享/上传就泄露）。
用凭据库集中保管，工作流里只引用名称，运行时由后端解密注入。

### 管理凭据

\`\`\`
全局设置 →「凭据库」标签：
- 新增凭据：填名称、说明、若干字段（字段名 → 值），如
    名称：我的邮箱    字段：value / password / api_key ...
- 列表只显示打码后的字段，不回显明文
- 编辑时字段值留空表示保留原值
\`\`\`

### 在节点里引用

\`\`\`
在任意节点的字符串里写：
  {{cred:名称}}          → 取该凭据的默认字段 value
  {{cred:名称.字段名}}   → 取指定字段，如 {{cred:我的邮箱.password}}
运行时自动替换为真实值；工作流文件、导出文件中都不含明文。
\`\`\`

> 凭据用 Fernet（AES）加密落盘，密钥与数据分离存放。计划任务的通知渠道口令同样支持该引用。

---

## 🧹 运行录像 / 数据留存清理

长期使用后运行录像与采集数据会占磁盘。可设置自动滚动清理。

\`\`\`
全局设置 →「留存清理」标签：
- 启用自动清理（默认开）
- 录像保留天数 / 录像总大小上限(MB)
- 数据保留天数 / 数据总大小上限(MB)
- 清理间隔(小时)
- 「立即清理一次」手动触发；页面显示当前占用
（0 表示该维度不限制；只清理导出文件与执行快照，配置文件受白名单保护不会被删）
\`\`\`

---

## 🌳 版本历史（VSCode 式分支图）

为工作流保存快照，像 Git 一样回溯。

### 入口

\`\`\`
顶栏独立「版本」按钮（紫色提交图标）
\`\`\`

### 用法

\`\`\`
- 提交当前版本：填可选说明 →「提交当前版本」生成一个快照节点
- 分支图：竖线贯穿，顶部「工作区(HEAD)」是当前未提交的画布，下面是历史版本圆点
- 每个版本可：恢复 / 与当前画布对比差异 / 删除
- 导出/导入「版本分享包」：把全部版本打包给团队
\`\`\`

> 版本历史是线性快照（非多分支合并）：每次提交都是一份完整快照，恢复即整体回到那一版。

---

## 🔗 相关文档

- [自定义模块](custom-modules-guide) - 封装与在线社区
- [计划任务系统](scheduled-tasks-guide) - 失败/成功通知
- [选择器完全指南](selector-guide) - 测试定位
- [自动化浏览器](browser-guide) - 网页智能录制
`
