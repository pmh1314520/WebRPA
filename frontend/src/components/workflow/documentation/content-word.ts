export const wordContent = `# Word 自动化指南

WebRPA 的 Word 自动化基于 **Word COM 接口**（而不是 python-docx），因此能表达「光标位置」
「当前选区」「所见即所得排版」这类真实 Word 语义，也支持导出 PDF。

本机安装 **Microsoft Word** 或 **WPS Office** 任意一个即可，系统会自动选择可用的引擎，
并在日志中回显实际用的是哪个（如「引擎: Microsoft Word」）。

## 核心用法：一次打开，多个模块连续操作

Word 模块是「会话式」的，典型链路：

\`\`\`
打开/新建Word  →  替换Word文本  →  保存Word  →  关闭Word
\`\`\`

「打开/新建Word」会建立一个文档会话，后续所有 Word 模块通过**文档标识（docKey）**
引用同一个文档。只处理一个文档时保持默认值 \`default\` 即可，无需关心。

> 只有「打开/新建Word」会创建会话。如果直接用其它 Word 模块，会提示「当前没有已打开的
> Word 文档」——先放一个「打开/新建Word」即可。

### 同时操作多个文档

给每个文档起不同的标识：

\`\`\`
打开/新建Word（文档标识: src，路径: D:\\\\源文件.docx）
打开/新建Word（文档标识: dst，路径: D:\\\\目标.docx）
读取Word文本（文档标识: src）→ 变量 word_text
写入文本至Word（文档标识: dst，内容: {word_text}）
关闭Word（勾选「关闭所有已打开的 Word 文档」）
\`\`\`

## 模块清单

| 模块 | 说明 |
|------|------|
| 打开/新建Word | 建立文档会话。文件存在则打开，不存在则新建（可关闭「文件不存在时新建」） |
| 读取Word文本 | 读全文 / 全部段落（列表）/ 指定段落 / 当前选区 |
| 写入文本至Word | 追加到文末 / 光标处插入 / 覆盖全文，可设字体字号加粗斜体 |
| 定位Word光标 | 文档开头、结尾、指定段落、查找到的文本处（可选中匹配文本） |
| 移动Word光标 | 按字符/单词/句/行/段落移动，可同时选中经过的内容 |
| 替换Word文本 | 查找替换，支持全字匹配、区分大小写、通配符 |
| 读取Word表格 | 把表格读成二维数组，或首行作表头读成字典列表（便于直接写 Excel） |
| 插入Word表格 | 用二维数组/字典列表填充，也可只指定行列数插空表 |
| Word插入图片 | 在光标处或文末插图，可指定宽高、居中 |
| Word插入超链接 | 插入链接，可设显示文字与悬停提示 |
| 保存Word | 原地保存，或填「另存为路径」存成新文件 |
| Word导出PDF | 导出已打开的文档，或直接给源文件路径独立转换（无需先打开） |
| 关闭Word | 关闭文档并退出引擎进程，可选择是否保存改动 |

## 文档被占用怎么办

Word/WPS 打开文档时会在同目录生成隐藏锁文件 \`~$文档名.docx\`。WebRPA 打开前会做判断，
两种情况处置完全不同：

- **文档确实正被另一个 Word/WPS 窗口编辑**：按「文档被占用时」配置处理。默认「自动以
  只读方式打开」，读取类流程照常跑通；选「直接报错停止」则立即失败。
- **只剩上次异常退出残留的锁文件**（文件其实可写）：自动清理该锁文件，按你原本的
  可写方式打开，日志会提示「已清理上次异常退出残留的锁文件」。

> 只读打开时，替换 / 写入 / 插表 / 插图 / 插链接这些写入类模块会**直接报错**，
> 而不是假装成功。因为只读文档上的修改不会落盘，报"成功"反而更误导。

## 常见问题

**替换报告成功，但文件没变？**
先确认文档不是只读打开的（日志里搜「只读」）。另外检查文档是否开启了「审阅 → 限制编辑」
保护。替换模块在执行后会回头校验一次，真没改动会直接报错，不会静默通过。

**「打开/新建Word」卡很久？**
首次拉起 Word 进程本身就慢（默认超时给了 2 分钟）。如果一直卡住，通常是 Word 弹了模态
对话框（文件被占用、需要密码、格式转换确认）。日志会分两步打印「正在启动 Word / WPS」
和「引擎已就绪，正在打开文档」，据此判断卡在哪一步。

**导出 PDF 失败？**
WPS 个人免费版对 COM 导出 PDF 有功能限制。替代路径：先用「保存Word」存成 .docx，
再用「通用文档转换」转 PDF；或本机安装 Microsoft Word 后重试。

**工作流中途失败，Word 进程会残留吗？**
不会。工作流结束（含失败、停止、超时）时会自动收尾：可写且已落盘的文档保存后关闭，
只读文档和从未保存过的新文档不保存关闭（避免弹「另存为」把流程卡死），
并确认引擎进程已退出。

**新建的文档保存时报「尚未保存到磁盘」？**
「打开/新建Word」没填路径时创建的是未命名文档，没有磁盘位置。用「保存Word」填
「另存为路径」，或在「打开/新建Word」里就指定文件路径。

## 实战：批量填充合同模板

\`\`\`
读取Excel（D:\\\\客户名单.xlsx）→ 变量 rows
遍历列表（rows）
  ├─ 打开/新建Word（路径: D:\\\\合同模板.docx）
  ├─ 替换Word文本（查找: {{客户名}}，替换为: {item[name]}）
  ├─ 替换Word文本（查找: {{金额}}，替换为: {item[amount]}）
  ├─ 保存Word（另存为路径: D:\\\\合同\\\\{item[name]}.docx）
  └─ 关闭Word（不保存改动）
\`\`\`

关键点：用「另存为路径」输出到新文件，模板本身不会被改动；循环里每轮都要「关闭Word」，
否则会累积多个文档会话和 Word 进程。
`
