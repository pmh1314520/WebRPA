export const aiAssistantGuideContent = `# AI 小助手指南

WebRPA 内置一个**全能 AI 小助手**，它不仅能回答问题，还能直接操作整个 WebRPA。任何你能在界面上手动做的事，它都能替你做。

---

## 快速开始

### 打开小助手

按 **Ctrl + K** 即可呼出小助手对话窗。或点击右上角的小助手按钮。

### 配置 AI 模型

进入 **全局配置 -> AI 助手** 配置：

| 字段 | 说明 |
|------|------|
| API 地址 | OpenAI 兼容地址（支持智谱 GLM、DeepSeek、通义千问、Kimi、本地 Ollama 等） |
| API Key | 你的密钥 |
| 模型 | 例如 \`glm-4-plus\`、\`deepseek-chat\`、\`qwen-plus\`、\`gpt-4o\` |
| 系统提示词 | 可选，给助手追加风格化指令 |
| 启用工具 | 是否允许助手自动调用 Skills 操作 WebRPA |
| 自动批准 | 危险操作（删除等）是否需要确认 |

> 兼容 OpenAI 协议的模型都能用。地址不带 \`/chat/completions\` 时会自动补全。

---

## 它能做什么

小助手具备**至高权限**，可以代替你完成下面所有操作：

### 工作流操作
- 新建空白工作流、保存当前工作流、导出 JSON / Markdown / Playwright 代码
- **有头/无头模式运行**当前工作流、停止运行
- 加载本地工作流、重命名工作流
- 拿到当前画布的完整节点 / 连线 / 变量详情

### 节点操作
- 添加节点、删除单个或批量节点
- 修改节点配置（任意字段）
- 把节点移动到精确坐标、修改节点显示名
- 批量按 type 查找节点 id
- 创建/删除连线，整理画布对齐
- 复制粘贴、撤销重做
- 自动适配视图（fit_view）、聚焦到指定节点

### 变量管理
- 新增、更新、删除、重命名变量
- 列出所有变量

### 资源与配置
- 列出本地工作流、自定义模块、计划任务
- 列出 Excel 资源、图像资源
- 读取/修改任何全局配置（浏览器、AI、邮件、数据库、QQ、飞书 等）
- 导出日志、下载数据表格、上传 Excel/图片

### 弹窗与界面
- 打开/关闭：全局配置、本地工作流列表、计划任务、文档、工作流仓库、自动化浏览器、手机投屏、变量追踪、屏保弹幕、导出对话框
- 切换底栏 Tab（日志/数据/变量/资源/图像）
- 触发系统截图工具
- 显示提示消息

### 长期记忆
- \`remember\` 记录用户偏好和习惯
- \`recall\` 跨会话查询记忆
- 下次会话开始自动注入最近 5 条记忆

### 全局快照（深度查询）
- \`get_full_snapshot\` 一次拿到执行器列表、本地工作流、自定义模块、计划任务、最近执行结果
- \`search_in_workflows\` 全文搜索所有本地工作流
- \`summarize_workflow\` 自动产出工作流结构化摘要（入口/出口节点、变量使用）

---

## WebRPA 管家能力（运维 / 企业 / 打包）

小助手不止会搭流程，更是 WebRPA 的"管家"：平台里一切 AI 能解决、能操作、能查到的事，都可以交给它。

### 能力自省
- \`list_my_capabilities\`：让助手列出自己当前掌握的全部技能分类与数量，"你都能干啥"一问便知

### 平台体检
- \`run_platform_health_check\`：一键自检平台健康度，输出 \`[严重]/[注意]/[提示]\` 分级报告（依赖、配置、运行时、数据完整性等），帮你快速定位隐患

### 运行数据聚合查询
- 聚合查询近期工作流执行情况：成功/失败次数、耗时、失败节点、采集数据量等
- 直接问"最近哪些流程跑挂了、为什么"，助手汇总执行历史给你结论

### 一键打包为 EXE
- 对助手说"把当前工作流打包成名为 XXX 的 exe"，它会调用打包能力，自动带上全局变量、Excel/图片资源、自定义模块，产出可独立运行的 Windows 程序（详见「平台能力」文档）

### 企业平台管理（如已启用企业模块）
- 查询/管理机器人集群、编排任务、审批流、审计日志、RBAC 角色与凭据保险库等
- 把"创建 -> 审批 -> 执行"的企业级闭环操作交给助手用自然语言驱动

> 这些能力都受「AI 操作权限」三档（逐项确认 / 智能放行 / 自由执行）约束，高风险操作会先弹授权。

---

## 使用示例

### 示例 1：让助手代你搭工作流

\`\`\`
你：帮我做一个工作流，每天早上 9 点自动打开微博，搜索"AI 新闻"，
    把前 10 条标题、内容、链接采集到 Excel

助手：好的。我先了解一下当前项目状态…
[调用 get_full_snapshot]
[调用 search_modules('采集')]

我打算这样设计：
1. 计划任务触发器（每天 9:00）
2. 打开浏览器 -> 操作已打开网页
3. 跳转到 https://s.weibo.com/weibo?q=AI 新闻
4. 循环 10 次：采集标题/内容/链接
5. 写入 Excel

我现在生成节点并装到画布里…
[调用 client_action(action='load_workflow_from_data', payload={...})]
[√] 已生成完成。请检查画布，运行试试看！
\`\`\`

### 示例 2：让助手批量改配置

\`\`\`
你：把所有"打开网页"节点的等待时间都改成 3 秒

助手：[调用 find_nodes_by_type('open_page')]
找到 5 个匹配节点，现在批量更新…
[依次调用 update_node_config]
[√] 5 个节点的 waitTime 已更新为 3
\`\`\`

### 示例 3：自然语言查询

\`\`\`
你：当前画布上一共多少个节点？哪些节点没连线？

助手：[调用 client_action(action='get_workflow_detail')]
当前画布共 12 个节点：
- 已连线：10 个
- 孤立节点：2 个（id: abc123 [click_element]、def456 [type_text]）
建议把它们连上或删除。
\`\`\`

### 示例 4：长期记忆

\`\`\`
你：记一下，我项目代码都放在 D:/Projects/，B站登录用 Edge 浏览器

助手：[调用 remember(content="项目目录 D:/Projects/，B站登录用 Edge", tags=["偏好"])]
已记下。下次帮你下载 B站 视频时会自动用 Edge 浏览器读 cookies
\`\`\`

---

## 美观回复

小助手的回复全程渲染 **Markdown / GFM**：
- 标题 / 列表 / 引用
- **粗体** *斜体* \`行内代码\`
- 代码块带语法高亮
- 表格、链接、kbd 标记

---

## 权限与安全

- **危险操作**（删除工作流、删除记忆等）默认需要确认，可在全局配置里关闭确认
- 助手所有操作都会**实时反映在 UI 上**，便于查看和回退
- 所有节点修改都会进入 **撤销栈**，按 Ctrl+Z 即可回退

---

## 推荐配置

不同模型适合不同场景：

| 模型 | 推荐用途 |
|------|----------|
| GLM-4-Plus / DeepSeek-Chat | 性价比高，工具调用稳定 |
| GPT-4o / Claude Sonnet | 复杂工作流推理 |
| Qwen-Plus | 中文场景体验最好 |
| 本地 Ollama | 离线使用 |

---

## 故障排查

| 现象 | 解决 |
|------|------|
| 助手不回复 | 检查 API Key、API 地址是否正确 |
| 助手只说话不操作 | 在全局配置里勾选"启用工具" |
| 报错 "LLM 调用失败" | 检查模型名称是否正确，可能模型不支持 function calling |
| 操作完没反应 | 看底部日志，可能 action 名拼错或缺少 payload |

---

## 高效用法

1. **新会话首条提问**：先描述需求，让助手调用 \`get_full_snapshot\` 摸清现状
2. **复杂工作流**：让助手用 \`build_workflow\` 一次产出节点结构，再 \`load_workflow_from_data\` 装入
3. **重复任务**：用 \`remember\` 把项目约定记住，省去每次重新解释
4. **调试**：直接问"这个工作流有什么潜在问题"，助手会调 \`summarize_workflow\` 给出建议

---

## 工作流自愈（运行失败自动修复）

这是 2.0 的重磅能力：助手搭完工作流后会**主动试运行**，如果跑挂了，它会**自己进入"诊断 -> 修复 -> 重跑"的有界循环**（最多 3 轮），而不是把错误甩给你。

它会做的事：
- 运行工作流并真实等待跑完，调 \`auto_heal_workflow\` 一键聚合所有失败节点 + 生成可执行修复计划；
- **selector 选不到** -> 自动 \`probe_page\` / \`suggest_selector\` 重探真实选择器并替换；
- **执行超时** -> 自动调大超时或前置插入等待元素，或加重试；
- **取值路径错** -> 用 \`get_node_io_snapshot\` 看上一步真实产出结构后修正；
- **必填字段缺失** -> 查 schema 默认值自动补上；
- 修完**自动重跑验证**，直到全绿才向你报成功；只有"账号密码 / 下载到哪 / 用哪个文件"这类必须你提供的信息，才会精准反问。

> 想体验：故意把某个网页节点的 selector 写错再让助手运行，看它如何自动 probe 重探并修复重跑。

---

## AI 数据处理模块（让工作流"会思考"）

助手不仅能搭流程，还能用上 2.0 新增的 **AI 数据处理工作流模块**（侧边栏「AI数据处理」分类），把大模型能力直接嵌进工作流：

| 模块 | 用途 |
|------|------|
| AI信息抽取 | 把网页/邮件/文档等非结构化文本抽成 JSON |
| AI文本分类 | 工单/评论自动归类（投诉/咨询/好评…） |
| AI文本摘要 / AI翻译 | 长文压缩 / 多语言翻译 |
| AI情感分析 | 评论舆情正负面判断 |
| AI数据规整 | 日期/金额/电话/地址统一标准格式 |
| AI语义去重 | 合并"表达不同但含义相同"的项 |
| AI智能路由 | 按内容智能选择后续分支，给工作流"判断力" |

典型组合：\`采集评论 -> AI情感分析 -> AI智能路由 -> 负面自动建工单 / 好评自动归档\`。这些模块默认复用全局 AI 配置，开箱即用。
`
