{
  "ui": {
    "批量上传失败: ${error}": "Batch upload failed: ${error}",
    "播放失败: ${err.message}": "Playback failed: ${err.message}",
    "创建工作流失败: ${createResult.error || '返回数据无效'}": "Failed to create Workflow: ${createResult.error || 'Invalid returned data'}",
    "附图${i + 1}": "Attachment ${i + 1}",
    "约 ${Math.round(intervalSeconds / 60)} 分钟": "About ${Math.round(intervalSeconds / 60)} minutes",
    "确定要删除文件夹\"${folderName}\"及其所有内容吗？": "Are you sure you want to delete the folder \"${folderName}\" and all its contents?",
    "导出出错: ${e}": "Export error: ${e}",
    "# 附件：${d.name}n${d.text}": "# Attachment: ${d.name}n${d.text}",
    "小助手正在：${currentActivity}…（可在下方再次发送来打断）": "Assistant is: ${currentActivity}… (send again below to interrupt)",
    "已思考 ${durationSec.toFixed(1)} 秒": "Thought for ${durationSec.toFixed(1)} seconds",
    "确定要删除任务\"${task.name}\"吗？此操作无法撤销。": "Are you sure you want to delete the task \"${task.name}\"? This action cannot be undone.",
    "一次性 - ${trigger.start_date} ${trigger.start_time}": "One-time - ${trigger.start_date} ${trigger.start_time}",
    "每天 ${trigger.daily_time}": "Daily ${trigger.daily_time}",
    "每周 ${dayLabels} ${trigger.weekly_time}": "Weekly ${dayLabels} ${trigger.weekly_time}",
    "每月 ${trigger.monthly_day}日 ${trigger.monthly_time}": "Monthly on day ${trigger.monthly_day} ${trigger.monthly_time}",
    "每隔 ${trigger.interval_seconds} 秒": "Every ${trigger.interval_seconds} seconds",
    "热键: ${trigger.hotkey}": "Hotkey: ${trigger.hotkey}",
    "启动后 ${trigger.startup_delay || 0}秒": "After startup ${trigger.startup_delay || 0} seconds",
    "确定要清空任务\"${task.name}\"的所有执行日志吗？此操作无法撤销。": "Are you sure you want to clear all execution logs of the task \"${task.name}\"? This action cannot be undone.",
    "${minutes}分${secs}秒": "${minutes}m ${secs}s",
    "已在设备 ${targetDeviceId} 上点击坐标 (${x}, ${y})": "Clicked coordinates (${x}, ${y}) on device ${targetDeviceId}",
    "测试失败: ${result.data?.error || result.error || '未知错误'}": "Test failed: ${result.data?.error || result.error || 'Unknown error'}",
    "启动拾取失败：${startRes.data?.error || startRes.error || '未知错误'}": "Failed to start picking: ${startRes.data?.error || startRes.error || 'Unknown error'}",
    "已拾取坐标 (${x}, ${y})": "Picked coordinates (${x}, ${y})",
    "启动拾取异常：${(e as Error)?.message || e}": "Picking start exception: ${(e as Error)?.message || e}",
    "来自「${(data.name as string) || moduleTypeLabels[moduleType as keyof typeof moduleTypeLabels] || moduleType}」": "From \"${(data.name as string) || moduleTypeLabels[moduleType as keyof typeof moduleTypeLabels] || moduleType}\"",
    "你是一个专业的${languageName}代码助手，专门为WebRPA工作流自动化平台编写代码。\r\n\r\n# 工作流环境信息\r\n\r\n## 编程语言\r\n${languageName}\r\n\r\n## 可用变量\r\n工作流中当前定义的所有变量：\r\n${variableList}\r\n\r\n## 变量访问方式\r\n${language === 'javascript' ? ": "You are a professional ${languageName} code assistant, dedicated to writing code for the WebRPA workflow automation platform.\r\n\r\n# Workflow Environment Info\r\n\r\n## Programming Language\r\n${languageName}\r\n\r\n## Available Variables\r\nAll variables currently defined in the workflow:\r\n${variableList}\r\n\r\n## Variable Access Method\r\n${language === 'javascript' ? ",
    "}\r\n\r\n## 代码执行环境\r\n${language === 'javascript' ? (moduleType === 'inject_javascript' ? ": "}\r\n\r\n## Code Execution Environment\r\n${language === 'javascript' ? (moduleType === 'inject_javascript' ? ",
    "用户当前的代码：\r\n```${language}\r\n${currentCode}\r\n```\r\n": "User's current code:\r\n```${language}\r\n${currentCode}\r\n```\r\n",
    "例如：${language === 'javascript' \r\n                    ? '计算两个数字的和并返回结果' \r\n                    : '读取文件内容并统计行数'}": "For example: ${language === 'javascript' \r\n                    ? 'Calculate the sum of two numbers and return the result' \r\n                    : 'Read file contents and count the lines'}",
    "自动化浏览器(${browserName})已打开，登录状态将自动保存": "Automation browser (${browserName}) is open; login state will be saved automatically",
    "导航失败: ${result.error}": "Navigation failed: ${result.error}",
    "已导航到: ${url}": "Navigated to: ${url}",
    "导航异常: ${error}": "Navigation exception: ${error}",
    "启动选择器失败: ${result.error}": "Failed to start selector: ${result.error}",
    "启动选择器异常: ${error}": "Selector start exception: ${error}",
    "出错回流「${p.targetId ? nodeLabel(p.targetId) : '上层'}」×${p.maxRetries ?? 1}": "Error reflow \"${p.targetId ? nodeLabel(p.targetId) : 'Upper level'}\" ×${p.maxRetries ?? 1}",
    "在模拟器/手机的WiFi设置中配置代理：代理地址填写本机IP，端口填写 ${port}": "Configure the proxy in the emulator/phone WiFi settings: enter this machine's IP as the proxy address, and enter ${port} as the port",
    "滚轮 ${(action.delta || 0) > 0 ? '向上' : '向下'} ${Math.abs(action.delta || 0)}": "Scroll wheel ${(action.delta || 0) > 0 ? 'Up' : 'Down'} ${Math.abs(action.delta || 0)}",
    "第 ${selectedRow + 1} 行": "Row ${selectedRow + 1}",
    "留空则使用: ${devices[0].id}": "Leave blank to use: ${devices[0].id}",
    "手势\"${recordingGestureName}\"录制成功！nn现在可以在下拉列表中选择使用该手势。": "Gesture \"${recordingGestureName}\" recorded successfully!nnYou can now select and use this gesture from the dropdown list.",
    "录制手势失败nn错误信息：${errorMsg}nn请确保：n1. 摄像头已连接n2. 已安装mediapipe库n3. 摄像头未被其他程序占用": "Failed to record gesture.nnError message: ${errorMsg}nnPlease ensure:n1. The camera is connectedn2. The mediapipe library is installedn3. The camera is not in use by another program",
    "摄像头 ${gestureStatus.camera_index} 已就绪": "Camera ${gestureStatus.camera_index} is ready",
    "准备录制手势\"${recordingGestureName}\"": "Ready to record gesture \"${recordingGestureName}\"",
    "确定要删除自定义手势\"${deleteGestureName}\"吗？nn删除后将无法恢复，需要重新录制。": "Are you sure you want to delete the custom gesture \"${deleteGestureName}\"?nnOnce deleted it cannot be recovered and will need to be recorded again.",
    "正在启动元素选择器，URL: ${resolvedUrl}": "Starting element selector, URL: ${resolvedUrl}",
    "启动失败: ${result.error}": "Start failed: ${result.error}",
    "找到 ${similar.count} 个相似元素": "Found ${similar.count} similar elements",
    "启动元素选择器失败: ${error}": "Failed to start element selector: ${error}",
    "已设置相似元素选择器，变量 ${variableName} 范围: ${similarResult.minIndex}-${similarResult.maxIndex}": "Similar element selector set; Variable ${variableName} range: ${similarResult.minIndex}-${similarResult.maxIndex}",
    "启动失败: ${result.error || result.data?.message}": "Start failed: ${result.error || result.data?.message}",
    "控件类型: ${element.control_type}": "Element type: ${element.control_type}",
    "类名: ${element.class_name}": "Class name: ${element.class_name}",
    "已捕获桌面元素: ${info}": "Desktop element captured: ${info}",
    "启动桌面元素选择器失败: ${error}": "Failed to start desktop element selector: ${error}",
    "测试失败：${res.error || d?.error || '未知错误'}": "Test failed: ${res.error || d?.error || 'Unknown error'}",
    "（经自愈候选 ${d.matchedSelector}）": "(via self-healing candidate ${d.matchedSelector})",
    "，首个文本：${d.element.text}": ", first text: ${d.element.text}",
    "命中 ${d.count} 个元素${via}${txt}，已在页面高亮": "Matched ${d.count} elements${via}${txt}, highlighted on the page",
    "测试选择器出错：${e}": "Error testing selector: ${e}",
    "已创建「${module.display_name || module.name}」的副本": "Created a copy of \"${module.display_name || module.name}\"",
    "桌面录制启动失败: ${res.data?.error || res.error}": "Desktop recording failed to start: ${res.data?.error || res.error}",
    "已根据桌面录制生成 ${newNodes.length} 个节点": "Generated ${newNodes.length} Nodes from desktop recording",
    " | 结束时间: ${new Date(exec.endTime).toLocaleTimeString()}": " | End time: ${new Date(exec.endTime).toLocaleTimeString()}",
    "删除凭据「${name}」？引用它的工作流将无法解析。": "Delete credential \"${name}\"? Workflows referencing it will fail to resolve.",
